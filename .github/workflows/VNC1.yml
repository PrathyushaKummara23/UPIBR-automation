name: Open RealVNC
on: workflow_dispatch
jobs:
  opening-real-vnc:
    runs-on: self-hosted
    steps:
    - name: Check System Info
      run: |
        echo "Current OS:"
        uname -a
        echo "Current directory:"
        pwd
        echo "Available shells:"
        cat /etc/shells
        echo "Environment:"
        env
