name: Test Workflow

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: 'Test input'
        required: true
        type: string

jobs:
  test:
    runs-on: [self-hosted, node16]
    steps:
      - name: Test step
        run: echo "Input was: ${{ github.event.inputs.test_input }}"
